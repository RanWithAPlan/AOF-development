ETH_defeat_france_category = {
    ETH_another_peace_conference = {
        visible = {
            FRA = { has_country_flag = refused_peace_deal }
        }
        availible = {
            FRA = { has_country_flag = refused_peace_deal }
            863 = { is_controlled_by = ETH }
		    268 = { is_controlled_by = ETH }
        }
        cost = 0
        days_remove = 10
        fire_only_once = yes
         complete_effect = {
            FRA = { country_event = ETH_events.1 }    
        }
}